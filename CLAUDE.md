# CODE-LEARN

HTML/CSS/JS를 방문자가 아무 설치·설정 없이 브라우저에서 바로 연습할 수 있는 정적(static) 웹사이트.
빌드 도구, 프레임워크, 백엔드 없음 — 순수 HTML/CSS/JS 파일만으로 동작하며, 그대로 GitHub Pages 같은 정적 호스팅에 올릴 수 있어야 한다.

## 저장소 정책 (중요, 예외 없음)

- **git commit/push는 매번 사용자가 명시적으로 요청했을 때만 실행한다.** 같은 대화 세션 안에서 이전에 허락받았더라도, 다음 commit/push는 다시 물어본다.
- 커밋 메시지, PR, 코드 주석, 사이트 문구 어디에도 **AI/Claude가 작업했다는 흔적을 남기지 않는다** (예: `Co-Authored-By: Claude` 금지).
- **작업은 `main`이 아니라 브랜치에 커밋한다. 브랜치 이름은 항상 그날 날짜만 쓴다** (예: `2026-07-18`) — 뒤에 어떤 설명도 붙이지 않는다(사용자가 명시적으로 지적한 규칙, 2026-07-18). 같은 날 두 번째로 작업을 시작해도 그 날짜 브랜치를 그대로 이어서 쓴다(새로 안 만듦).
- **PR을 만들거나 `main`에 합치는 건 Claude가 절대 하지 않는다 — 그건 항상 사용자가 직접 한다.** Claude는 브랜치에 커밋하고 push까지만 한다.

## 폴더 구조

```
index.html          홈 — "자유 연습" / "미션으로 배우기" 진입점
sandbox.html         자유 연습(샌드박스) 페이지
missions.html        미션 목록 페이지
mission.html         미션 실행 페이지 (?id=미션ID 로 어떤 미션인지 결정, 미션마다 별도 HTML 파일을 만들지 않음)
assets/css/style.css 사이트 전체 공용 스타일
assets/js/editor.js  재사용 가능한 코드 입력 + 실시간 미리보기 컴포넌트 (createPlayground, 보여줄 칸을 panels로 고를 수 있음)
assets/js/missions-data.js  missions/ 안의 미션 파일들을 커리큘럼 순서로 import해서 MISSIONS 배열로 조립 (ES 모듈)
assets/js/missions/helpers.js  미션 채점에 쓰는 CSS 파싱 공용 헬퍼 (여러 미션 파일이 import해서 씀)
assets/js/missions/<미션id>.js  미션 하나당 파일 하나 (제목/설명/시작 코드/채점 함수, default export)
assets/js/storage.js  저장소 어댑터 (진행도/코드 저장 — 지금은 localStorage, 서버 전환 시 여기만 교체)
assets/js/mission-page.js   mission.html 전용 로직 (데이터 로드, 채점, storage.js 호출)
CLAUDE.md            이 문서
MEMORY.md            현재 진행 상태 요약 (짧게 유지)
```

## 아키텍처 핵심 결정

- **미션은 데이터 기반**: `assets/js/missions/`에 미션 하나당 파일 하나를 만들면 새 미션이 생긴다. 미션마다 새 HTML 파일을 만들지 않는다 — `mission.html?id=xxx` 하나로 전부 처리. 이렇게 해야 미션이 수십 개로 늘어나도 저장소가 비대해지지 않는다.
- **미션 파일이 30개를 넘어가면서 전부 `missions-data.js` 하나(1500줄+)에 몰려있던 걸 미션당 파일 하나로 쪼갬(2026-07-18, 사용자가 명시적으로 지적한 문제).** 빌드 도구가 없어서(위 원칙) 브라우저 네이티브 **ES 모듈**(`import`/`export`)로 나눴다 — `missions-data.js`가 `missions/` 안의 각 파일을 커리큘럼 순서로 import해서 `MISSIONS` 배열로 조립하고 `export`한다. **이렇게 하면 트레이드오프가 하나 생긴다: ES 모듈은 `file://`로 HTML을 직접 더블클릭해서 열면 CORS 때문에 동작하지 않는다 — 로컬에서 확인할 때 반드시 로컬 서버를 띄워서 `http://`로 접속해야 한다** (GitHub Pages 같은 실제 호스팅은 `https://`라서 전혀 문제없음). 이 트레이드오프를 사용자에게 미리 확인받고 진행한 것 — 대안(`document.write` 기반 동적 로더, `file://`에서도 동작하지만 구식 느낌)도 검토했으나 ES 모듈 쪽을 선택함.
  - `missions-data.js`를 import하는 쪽(`missions.html`의 인라인 스크립트, `mission-page.js`)도 전부 `<script type="module">`이어야 한다. `editor.js`/`storage.js`는 굳이 모듈로 안 바꿔도 된다 — 모듈이 아닌 일반 `<script>`가 만든 전역 함수(`createPlayground`, `getProgress` 등)는 모듈 스크립트에서도 그냥 보이기 때문에, `mission-page.js`가 모듈이 되어도 그 전역들은 import 없이 그대로 쓸 수 있다. 모듈 스크립트는 문서 파싱이 끝난 뒤 실행되므로(`defer`와 비슷), 그 전에 실행되는 일반 스크립트들의 전역이 먼저 채워져 있는 게 보장된다.
  - 디버깅 편의를 위해 `missions-data.js`가 `window.MISSIONS = MISSIONS;`로 전역에도 브릿지해둔다 — 콘솔에서 바로 확인 가능. 다른 스크립트가 이 전역에 의존하진 않고, 전부 `import`로 명시적으로 가져다 쓴다.
- **실행 미리보기(iframe)는 보안상 `sandbox="allow-scripts"`만 사용하고 `allow-same-origin`은 절대 함께 주지 않는다.** 방문자가 입력한 임의의 JS를 실행해야 하므로, 이 조합을 깨면 iframe 안 스크립트가 부모 페이지(진짜 사이트)에 접근할 수 있게 되어 위험하다.
  - `alert`/`confirm`/`prompt`는 `sandbox`에 `allow-modals`를 추가로 넣어야 뜬다(2026-07-24, 사용자가 실제로 겪고 발견) — `allow-scripts`만으로는 스펙상 모달 호출이 조용히 막힌다. `editor.js`/`advanced-editor.js` 양쪽 iframe에 추가함.
  - **`localStorage`/`sessionStorage`/`document.cookie`/`indexedDB`는 이 iframe 안에서 절대 쓸 수 없다 — sandbox 토큰을 아무리 추가해도 못 고친다(2026-07-24, 헤드리스 크롬으로 직접 검증함, 전부 `SecurityError: The document is sandboxed and lacks the 'allow-same-origin' flag` 발생).** `allow-same-origin` 없는 iframe은 origin 자체가 없는 상태(opaque origin)라서 저장할 곳이 없기 때문 — 이 넷은 전부 origin에 묶여있어서 위 보안 원칙(`allow-same-origin` 금지)을 깨지 않는 한 근본적으로 못 푸는 문제다. **미션의 starter/instructions/데모가 이 네 API 중 하나라도 실제로 호출하게 만들면 안 된다** — 학습자가 실행 버튼을 눌러보면 코드가 완벽히 맞아도 조용히 에러가 난다(관련해서 실제로 `js-localstorage-intro` 미션을 만들었다가 이 문제를 발견하고 삭제함). localStorage 개념 자체를 가르치고 싶으면 이 사이트가 `storage.js`에서 실제로 그 방식을 쓰고 있다는 걸 글로 설명하는 것까지만 가능하고, 연습장에서 직접 실행해보게 하는 미션은 만들 수 없다.
- **미션 채점은 iframe의 렌더링 결과가 아니라 코드 문자열 자체를 파싱해서 검사한다.** (`missions-data.js`의 각 `check(code)` 함수) iframe 내부 DOM을 부모에서 읽으려면 `allow-same-origin`이 필요한데, 위 보안 이유로 그걸 안 쓰기로 했기 때문.
  - HTML 채점: `DOMParser`로 파싱해서 태그/텍스트 확인.
  - CSS 채점: `missions-data.js`의 `parseCssRules(cssText)` / `findRuleForTag(cssText, tagName)` / `findRuleForClass(cssText, className)` 헬퍼를 사용. `CSSStyleSheet().replaceSync()`로 실행 없이 CSS만 파싱하므로(구형 브라우저는 정규식 폴백) iframe이나 실제 페이지 적용이 전혀 필요 없다. 단순히 선택자 문자열에 태그/클래스 이름이 "포함"되는지만 보면 안 된다 — `h1 + p`나 `h1 > p`처럼 콤비네이터가 있으면 실제로 스타일이 적용되는 건 h1이 아니라 p이기 때문에, 콤마(그룹 선택자)와 콤비네이터(공백, `>`, `+`, `~`)로 선택자를 쪼개서 **가장 오른쪽(실제로 적용되는) 조각**만 검사한다(`lastCompoundSelector` / `compoundSelectorHasTag` / `compoundSelectorHasClass`). 실제로 `h1 + p { font-size: 40px; }`를 h1 미션 정답으로 잘못 통과시키는 버그가 있었고(사용자가 발견) 이렇게 고쳤다 — 앞으로 선택자 채점 로직을 만질 땐 이 콤비네이터 문제를 꼭 염두에 둘 것.
  - JS 값 채점(예: 특정 변수/동작 확인)은 아직 없음 — 필요해지면 이 방식(문자열/AST 파싱, 실행 없이)을 우선 검토할 것. 사용자 코드를 실제로 `eval`하거나 iframe에서 실행 결과를 부모가 읽는 방식은 보안 문제로 피한다.
  - **JS 문법 오류 사전 검사 — `mission-page.js`의 `findJsSyntaxError(code)`(2026-07-18, 사용자가 실제로 겪고 발견한 버그).** `check(code)`는 정규식으로 문자열 패턴만 보기 때문에, `function ("toggle-btn") { ... }`처럼 매개변수 자리에 문자열을 넣는 실수(문법 자체가 깨져서 스크립트 전체가 파싱 단계에서 실패 — 클릭해도 아무 반응이 없음)도 정규식 패턴만 맞으면 "통과"로 잘못 넘어갔다. 그래서 `checkBtn` 클릭 시 개별 미션의 `check()`를 부르기 **전에** `code.js`와 `code.html` 안 `<script>` 태그들을 `new Function(문자열)`로 공통 사전 검사한다 — 이건 문자열을 함수 "본문"으로 *파싱만* 하고 반환된 함수를 호출하지 않으므로, "실행 결과를 훔쳐보지 않는다"는 채점 보안 원칙을 안 건드린다(실제로 실행되는 게 아니라 문법이 유효한지만 확인함). **`advancedEditor`(ES 모듈, `import`/`export` 문법) 미션은 검사 대상에서 뺐다** — `new Function()`은 모듈 문법을 아예 못 받아들여서(정상적인 `export function ...`도 SyntaxError로 오탐함) 이 방식 그대로는 못 씀, 필요해지면 별도 검증 방법을 찾을 것.
  - **괄호/따옴표 자동 닫기 + type-over(2026-07-24 완료).** `maybeAutoCloseBracket`이 이제 `{`/`(`/`"`/`'` 전부를 열 때 닫는 짝을 자동으로 만든다. 별도로 `maybeTypeOverClosing`(닫는 문자 `}`/`)`/`"`/`'`를 직접 타이핑했을 때, 바로 뒤에 이미 같은 문자가 있으면 새로 삽입하지 않고 커서만 그 뒤로 넘기는 처리)을 추가해서 중복 괄호/따옴표가 쌓이는 문제를 막았다 — `{`도 기존엔 type-over가 없어서 같은 문제가 있었기에 같이 포함시켰다. 따옴표는 여는/닫는 문자가 같아서 "바로 뒤에 같은 문자가 있는지"로만 새로 열지/넘어갈지를 구분한다(둘 다 `editor.js`). `[`는 아직 요청받지 않아 대상에서 뺐다.
  - **전체화면에서 코드가 길어지면 위아래 스크롤이 전혀 안 되던 버그(2026-07-24, 사용자가 이 작업 중 직접 발견).** 원인: 전체화면에서 `.code-editor`가 `height:220px`(고정) 대신 `flex:1;height:auto`로 바뀌는데, flex 아이템의 기본 자동 최소 높이(`min-height:auto`)가 `overflow:visible`인 자식의 실제 콘텐츠 크기를 반영한다 — `.code-gutter`(줄 번호)는 `position:absolute`가 아닌 일반 흐름의 진짜 텍스트라서, 코드 줄이 많아지면 이 자동 최소 높이가 뷰포트보다 커져 `.code-editor`를 그만큼 억지로 키워버렸다. 그러면 넘치는 부분이 조상의 `height:100%`+`overflow:hidden`(`.playground-editors.tabbed` 등)에 잘려나가고, `textarea` 자신은 이미 내용에 맞게 커져버려서 내부 스크롤바도 안 생기는 이중 문제였다. `.code-editor`에 `min-height:0`을 추가해 고쳤다(`assets/css/style.css`) — **앞으로 `.code-editor`나 비슷하게 `flex:1;height:auto`로 전환되는 전체화면 레이아웃을 새로 만들 때, 그 안에 실제 텍스트 콘텐츠(절대 위치가 아닌)를 가진 자식이 있으면 이 자동 최소 높이 트랩을 반드시 의심할 것.**
  - **미리보기 실행 실패를 알려주는 "콘솔" 패널 완성(2026-07-24).** 예전엔 미리보기 `iframe` 안에서 에러가 나도 부모 페이지는 전혀 몰랐다(opaque origin이라 안을 못 들여다봄, 위 보안 원칙 참고). `postMessage`가 이 sandbox 조합에서도 되는지 헤드리스 크롬으로 먼저 스파이크 검증한 뒤(된다) 구현함: iframe의 srcdoc `<head>` 맨 앞에 `buildPreviewErrorBridgeScript()`(`editor.js`)가 만드는 부트스트랩 `<script>`를 심어서, `window.addEventListener("error"/"unhandledrejection", ...)`로 잡은 에러 메시지 문자열만(DOM이나 다른 내부 정보는 안 보냄) `window.parent.postMessage({ source: "code-learn-preview", message }, "*")`로 보낸다. 부모 쪽 `createPreviewConsole(iframe, consoleEl)`(역시 `editor.js`, 전역 함수라 `advanced-editor.js`도 그대로 재사용)이 `window`의 `message` 이벤트를 받아서 — opaque origin이라 `event.origin`은 항상 `"null"`이라 못 쓰고, 대신 **`event.source === iframe.contentWindow`로 비교해서 우리 iframe에서 온 메시지인지 판별**한다 — `.preview-console`(각 플레이그라운드의 미리보기 iframe 바로 아래, 터미널처럼 어두운 배경의 `<pre>`)에 한 줄씩 채워 넣는다. `run()`이 호출될 때마다(`editor.js`/`advanced-editor.js` 둘 다) `previewConsole.clear()`로 먼저 비우므로, 새로 실행할 때마다 이전 실행의 에러가 안 남는다. 비어있을 땐 CSS `.preview-console:empty::before`로 "에러 없음" 안내만 흐리게 보여준다.
  - **`border`/`padding`/`margin`/`font`/`background` 같은 축약(shorthand) 속성은 `parseCssRules`에서 별도 처리가 필요하다.** `CSSStyleSheet`의 `rule.style`을 순회(`rule.style[j]`)하면 축약 속성 이름 그대로는 절대 안 나오고, 브라우저가 자동으로 개별 속성(예: `border` → `border-top-width`, `border-top-style`, `border-top-color` ... 최대 17개)으로 풀어서 나열해버린다 — 그래서 `declarations["border"]`가 실제로는 `undefined`가 된다(값이 없어서가 아니라애초에 그 키로 안 잡히기 때문). `CSS_SHORTHAND_PROPS` 목록에 있는 이름들은 `rule.style.getPropertyValue(이름)`으로 따로 한 번 더 확인해서 `declarations`에 채워 넣는다. 박스모델 미션(`css-border-intro`/`css-padding-intro`/`css-margin-intro`)을 만들다가 실제로 겪은 버그(전부 실패 처리되고 있었음) — **앞으로 새로운 CSS 축약 속성(예: `flex`, `grid`, `transition`, `outline`)을 채점 대상으로 추가할 땐 반드시 `CSS_SHORTHAND_PROPS`에 추가할 것**, 안 그러면 조용히 항상 실패하는 채점 로직이 생긴다.
- 로그인/서버/DB 없음. 미션 완료 여부·코드는 방문자 브라우저의 `localStorage`에만 저장 (다른 사람과 공유되지 않는, 순전히 개인 기기 로컬 기록). 자세한 내용은 아래 "저장 방식" 절 참고.
- **미션은 이전 미션에서 작성한 코드를 이어받을 수 있다.** 미션 객체에 `startFromMission: "이전미션id"`를 넣으면, 그 이전 미션에 저장된 HTML을 이번 미션의 시작 HTML로 가져온다. 이건 **한 번 복사해오고 끝이 아니라 매번 진입할 때마다 다시 확인하는 실시간 연동**이다 — 방문자가 나중에 이전 미션으로 돌아가 HTML을 고치면, 이번 미션에 다시 들어왔을 때도 그 최신 HTML이 반영된다(사용자가 명시적으로 요청한 동작). 이전 미션에 저장된 게 없으면 `starter.html` 기본값을 쓴다. 반면 **CSS/JS는 이 미션 자체의 작업물**이라 이전 미션과 무관하게 이 미션에 저장된 값을 그대로 이어서 쓴다 — 이전 미션을 고쳤다고 해서 이 미션에서 이미 써둔 CSS가 사라지지 않는다. 예: `css-font-size-intro`는 `html-intro-card`에서 HTML만 이렇게 실시간으로 이어받는다.
- **미션의 starter 코드에 정답 골격을 미리 채워두지 않는다 (중요 — 사용자가 명시적으로 지적한 규칙).** 예시 코드는 `instructions`에 `<pre><code>`로 "읽고 보는 용도"로만 보여주고, 실제로 채점 대상이 되는 `starter.html`(그 미션에서 가르치는 태그/문법)은 비워두거나 `<!-- 여기에 입력해보세요 -->` 같은 안내 주석만 남긴다. 방문자가 예시를 눈으로 보고 **직접 타이핑**하게 만드는 게 목적이라, 텍스트만 살짝 바꾸면 통과되는 코드를 에디터에 미리 넣어두면 안 된다. 그 미션에서 가르치는 것과 무관한 보일러플레이트(예: 페이지 여백을 위한 기본 CSS)는 예외적으로 채워둬도 괜찮다.
  - **"직접 타이핑" 원칙을 기술적으로도 강제한다(2026-07-18, 사용자 지적).** `.mission-instructions pre`(예시 코드 블록)에 `user-select: none`을 걸어 마우스로 긁어 선택할 수 없게 하고, `mission-page.js`에서 그 블록들에 `copy` 이벤트를 `preventDefault`로 한 번 더 막는다(이중 방어). 새로 예시 코드를 보여주는 UI를 추가할 때도 이 두 가지를 같이 적용할 것 — 안 그러면 예시를 그대로 긁어 붙여넣어서 "직접 타이핑" 원칙이 무력화된다.
- **아직 안 배운 언어의 입력 칸은 화면에 아예 존재하면 안 된다 (중요 — 사용자가 명시적으로 지적한 규칙).** `createPlayground`의 `panels` 옵션으로 이 미션에서 보여줄 칸만 고른다 (`["html"]`, `["html","css"]`, `["html","css","js"]` 등). 예: HTML만 배운 시점의 미션은 `panels: ["html"]`이라 CSS/JS 칸 자체가 없고, 미리보기도 CSS가 전혀 적용 안 된 브라우저 기본 모습 그대로 보여야 한다(그래서 이 단계 미션의 `starter`에는 `css`/`js` 키를 아예 넣지 않는다). 이후 미션에서 새 언어가 추가될 때만 그 칸을 `panels`에 넣는다. `sandbox.html`(자유 연습)은 예외 — 처음부터 세 칸 다 보여준다 (기본값).
- **미션 순서는 HTML → CSS → JS로 점진적으로 배운다** (사용자가 지정한 커리큘럼 순서). 같은 언어 안에서도 쉬운 개념(태그 선택자로 속성 하나 바꾸기)부터 어려운 개념(클래스 선택자로 특정 요소만 골라 꾸미기)순으로 배치한다. `MISSIONS` 배열의 순서 = 학습 순서이므로, 새 미션을 끼워 넣을 땐 배열 중간에 넣어야 할 수도 있다(맨 끝에 추가하는 게 항상 맞는 게 아님).
- 미션은 `tier` 필드(`"입문"` / `"주니어"` / `"시니어"`)로 단계가 나뉜다. `missions.html`의 `TIERS` 배열과 이름이 맞아야 필터가 동작한다. 각 단계에서 방문자가 도달해야 하는 목표는 아래 "커리큘럼 단계별 목표" 절 참고.
- **데모 iframe(`srcdoc`)에 인라인 `<script>`나 중첩된 따옴표를 넣을 때 겪기 쉬운 함정 2가지(둘 다 실제로 겪은 버그, 2026-07-18 발견 및 수정):**
  1. **`<script>...</script>`를 그대로 문자열에 넣으면 안 된다.** `srcdoc` 값 자체가 우리 JS 소스 안에서 `"..."`로 감싸여 있는데, 그 안에 진짜 `</script>` 문자열이 그대로 들어가면 (이 `missions-data.js` 파일이 아니라) 최종적으로 브라우저가 그 `<script>` 태그를 의도보다 일찍 닫아버릴 수 있다. `<\\/script>`처럼 백슬래시로 이스케이프하는 건 **틀린 해결책**이다 — HTML엔 백슬래시 이스케이프가 없어서 그 백슬래시가 그냥 문자 그대로 남고, `<\/script>`는 진짜 닫는 태그로 인식이 안 돼서 스크립트가 절대 안 끝나고 결국 문법 오류로 조용히 실행 자체가 안 된다(`js-textcontent-intro`의 두 번째 데모가 실제로 이래서 화면이 안 바뀌고 있었다). **`editor.js`의 `run()`이 이미 쓰고 있는 문자열 분리 방식**(`"<" + "/script>"`)을 항상 쓸 것.
  2. **`srcdoc="..."`처럼 큰따옴표로 감싼 HTML 속성 안에서, 그 내용 중 또 큰따옴표가 필요하면(예: `eval('...background="tomato"...')`) 백슬래시로 이스케이프하면 안 된다.** HTML은 속성값 안의 따옴표를 이스케이프하는 문법이 없어서 — 백슬래시가 있든 없든 큰따옴표가 나오는 순간 그 자리에서 속성이 끝나버린다(실제로 `senior-eval-danger`/`senior-xss-textcontent` 데모에서 이 문제로 `srcdoc`이 중간에 잘려나갔다). 중첩된 부분에서 큰따옴표가 필요하면 `&quot;` HTML 엔티티를 쓸 것 — 브라우저가 속성값을 계산할 때 한 번만 디코딩해주므로, 그 다음부터는 안전한 일반 문자가 된다. 같은 이유로 HTML 속성 자체는 최대한 작은따옴표(`'`)를 써서 바깥 `srcdoc="..."`의 큰따옴표와 아예 안 겹치게 하는 게 제일 간단하다.
- **코드 에디터(HTML/CSS/JS 입력 칸)는 CodeMirror 같은 외부 라이브러리 없이 직접 구현한 문법 강조 + 줄 번호를 갖고 있다** (`assets/js/editor.js`). 방식: 투명한 `<textarea>`(caret-color만 보임) 밑에 정규식 기반으로 태그/키워드/문자열 등을 `<span>`으로 감싼 `<pre>`를 겹쳐놓는다. 이 강조 결과는 `tokenize()` 헬퍼가 만드는데, 정규식이 못 잡아낸 문자까지 전부 `escapeHtml`을 거치도록 되어 있다 — 이 `<pre>`는 (미리보기 iframe이 아니라) 사이트 자체 페이지에 `innerHTML`로 직접 들어가므로, 여기서 이스케이프를 빼먹으면 방문자가 타이핑한 내용이 사이트 자기 자신에 실제 HTML/스크립트로 끼어드는 self-XSS 구멍이 생긴다. **강조 로직을 고칠 땐 이 이스케이프 보장을 절대 깨지 말 것.** 이 오버레이 기법의 트레이드오프로 `resize`(칸 크기 수동 조절)는 지원하지 않는다 — textarea/강조 pre/줄번호 칸 셋의 높이가 항상 고정값(220px)으로 같아야 셋이 어긋나지 않기 때문이며, 대신 각자 내부 스크롤을 textarea의 `scroll` 이벤트로 나머지 둘에 맞춰준다(`syncScroll`). 자동완성/괄호매칭 같은 고급 기능은 없는 근사치 강조다 — 사용자가 CDN 라이브러리(CodeMirror 등)보다 이 방식을 명시적으로 선택함(외부 의존성 0개 유지 원칙).
  - **커서 위치가 실제 글자와 안 맞는 버그가 있었다(사용자가 발견, 2026-07-18 수정).** 원인: 브라우저 기본 스타일이 `<code>` 요소에 `font-family: monospace`를 강제로 넣어서, 부모 `.code-highlight`(pre)에서 지정한 폰트(`Consolas, D2Coding, monospace`)를 안 물려받고 다른 monospace 폰트로 렌더졌다. 이 테스트 환경에선 두 폰트가 우연히 같은 걸로 대체돼서 안 드러났지만, Consolas/D2Coding이 실제로 깔린 환경에서는 textarea와 강조 레이어의 글자 너비가 달라져 커서와 글자가 어긋났다. `.code-highlight code { font: inherit; }`로 강제 상속시켜 고침. **앞으로 강조 레이어에 새 자식 요소를 추가할 때마다, 브라우저 기본 스타일이 그 태그에 폰트를 강제하는 게 없는지(특히 `code`, `pre`, `kbd`, `samp` 등) 확인할 것** — 안 그러면 같은 버그가 다른 태그에서 또 생긴다.
  - **Enter 입력 시 자동 들여쓰기도 한다** (`editor.js`의 `handleEnterKey`, `keydown`에서 `preventDefault`하고 직접 처리 — `textarea.value`를 코드로 바꾸면 `input` 이벤트가 자동으로 안 나가므로, 호출부에서 `updateVisual`/`scheduleRun`을 직접 불러줘야 한다). 기본은 현재 줄의 들여쓰기를 새 줄에 그대로 유지하고, 커서 바로 앞이 여는 괄호(`{`/`[`/`(`)나 여는 태그(`<div>` 등, void 요소 제외)면 한 단계 더 들여쓴다. 거기에 더해 커서 바로 뒤에 정확히 짝이 되는 닫는 괄호/태그가 곧바로 있으면(예: `.box {}`나 자동 닫기로 생긴 `<div></div>`) 그 닫는 짝을 새 줄로 내려보내고 그 사이에 한 단계 들여쓴 빈 줄을 만든다. **닫는 짝이 아직 없는 경우(예: CSS에서 `{`만 치고 `}`는 나중에 치는 흔한 흐름)까지 감안해서, "닫는 짝이 바로 뒤에 있는지"와 "그냥 여는 것 뒤인지"를 반드시 따로 처리할 것** — 하나로 뭉뚱그리면 `{`만 친 뒤 Enter를 눌러도 들여쓰기가 전혀 안 되는 버그가 난다(실제로 한 번 겪었던 버그).
  - **닫는 괄호(`}`/`]`/`)`)를 직접 타이핑하면 자동으로 내어쓰기도 한다** (`maybeOutdentClosingBracket`). `{`만 치고 Enter로 한 단계 들여써진 상태에서 `}`를 치면, 그 줄이 원래(여는 줄과 같은) 들여쓰기로 한 단계 줄어든다 — `.box {\n  }`처럼 어긋나지 않고 `.box {\n}`처럼 맞는다. `maybeAutoCloseTag`와 마찬가지로 `inputEvent.inputType === "insertText"`로 진짜 타이핑인지 확인하고 나서만 동작한다(백스페이스 등으로 우연히 커서가 그 자리에 온 경우까지 건드리면 안 됨).
  - **textarea 안에서 Tab/Shift+Tab이 포커스 이동 대신 들여쓰기 삽입/제거로 동작한다** (`handleTabKey`, `keydown`에서 `preventDefault`). 브라우저 기본값은 Tab을 누르면 다음 포커스 가능한 요소로 넘어가버리는 것이라, 이걸 막지 않으면 코드 에디터 안에서 Tab으로 들여쓰기를 칠 수 없다(사용자가 실제로 겪고 지적한 문제).
- **HTML 칸은 여는 태그를 다 치면(`<h1>` 등) 닫는 태그(`</h1>`)를 자동으로 만들어준다** (`editor.js`의 `maybeAutoCloseTag`, void 요소 `br`/`img`/`input` 등과 이미 자기 자신을 닫은 태그 `<br />`는 제외). **반드시 `input` 이벤트의 `inputEvent.inputType === "insertText" && inputEvent.data === ">"`로 "진짜 방금 '>'를 타이핑했는지"를 확인하고 나서만 동작해야 한다** — 커서 앞 글자가 `>`인지만 보고 판단하면 안 된다. 실제로 `<h1>` 뒤에서 줄바꿈했다가 백스페이스로 그 줄바꿈만 지우면 커서가 다시 `<h1>` 바로 뒤(`>` 다음)로 돌아오는데, 이때도 "방금 `>`를 쳤다"고 착각해서 닫는 태그를 또 만들어버리고, 지울 때마다 반복되면 닫는 태그가 무한 복사되는 버그가 실제로 있었다(사용자가 발견) — `inputType` 체크로 고침. 앞으로 이 근처 로직을 고칠 땐 삭제 이벤트(`deleteContentBackward` 등)에서 오작동하지 않는지 반드시 확인할 것.
  - **`KNOWN_HTML_TAGS`(표준 HTML 태그 이름 목록)에 없는 태그 이름은 자동 닫기 대상에서 뺀다(사용자가 명시적으로 지적한 규칙, 2026-07-18).** 처음엔 `<foo>`처럼 존재하지도 않는(오타이거나 지어낸) 태그도 그럴듯하게 `</foo>`를 만들어 닫아줬는데, 이러면 방문자가 오타를 오타인 줄 모르고 넘어갈 수 있어서 문제였다 — 실제 표준 HTML 태그일 때만 자동 닫기가 동작해야 방문자가 잘못 타이핑한 걸 스스로 알아챌 수 있다. `KNOWN_HTML_TAGS`는 이 사이트가 지금 가르치는 태그보다 넓게(HTML 표준 태그 전체) 잡아뒀다 — 아직 커리큘럼에서 안 가르친 태그(예: `video`)라도 실제로 존재하는 태그라면 자동 닫기는 정상 동작해야 자연스럽다. 새로 표준에 추가된 태그를 지원해야 할 일이 생기면 이 목록에 추가할 것.
  - **알려진 한계(의도적으로 고치지 않기로 함, 2026-07-18)**: 이미 내용까지 채워진 완성된 태그(`<h1>hello</h1>`)에서 여는 태그의 `>`만 지웠다 다시 치면, 뒤에 `</h1>`가 이미 있는데도 또 하나 만들어서 `<h1></h1>hello</h1>`처럼 깨진다. 태그가 비어있는 경우(`<h1></h1>`에서 재입력)만 바로 뒤 문자열이 정확히 일치하는지 확인해서 중복을 막아뒀지만, 내용이 있는 일반적인 경우까지 잡으려면 여는/닫는 태그를 스택으로 추적하는 진짜 파서가 필요해서 하지 않기로 함 — 사용자가 VS Code도 이 경우 동일하게 깨지는 걸 확인하고 패스하기로 결정함. 나중에 이 근처를 다시 만질 일이 있어도 이 한계를 "버그"로 착각해서 무리하게 고치려 하지 말 것.
- **단일 파일 에디터의 `autoRun` 옵션(`createPlayground`, 2026-07-18 추가).** 새 고급 에디터를 만든 뒤 사용자가 직접 써보고 지적한 문제: 타이핑할 때마다 500ms 후 자동으로 미리보기가 갱신되는 기존 방식은, 태그를 다 닫기 전(예: `<p`만 친 상태)의 미완성 HTML까지 그대로 미리보기에 깜빡여 보인다. 이건 새 고급 에디터가 실행 버튼 방식이라 안 겪는 문제라, "그럼 전부 다 실행 버튼 방식으로 바꾸는 게 낫지 않냐"는 제안이 나왔음 — 하지만 **"타이핑하면 바로 옆에서 결과가 보인다"는 즉각 피드백은 입문 단계엔 핵심 가치**(홈 화면 문구 자체가 이거임)라 전부 바꾸는 대신, `createPlayground`에 `options.autoRun`(기본 `true`)을 추가해서 **입문 미션만 자동실행 유지, 주니어/시니어(단일 파일 에디터를 쓰는 일반 미션)는 실행 버튼을 눌러야만 미리보기가 갱신되도록** 나눴다(`mission-page.js`에서 `autoRun: mission.tier === "입문"`로 넘김). **저장(자동저장)은 이 옵션과 무관하게 항상 그대로 동작한다** — 실행 버튼을 안 눌러도 타이핑한 코드 자체는 이어서 자동 저장됨, 미리보기 갱신 여부만 다르다. autoRun이 꺼졌을 때 미리보기 툴바 라벨이 "미리보기 (실행 버튼을 눌러야 갱신돼요)"로 바뀌어서, 입문 단계에서 자동실행에 익숙해진 학습자가 갑자기 아무 반응이 없어도 당황하지 않게 안내한다. `sandbox.html`(자유 연습)은 `autoRun` 옵션을 안 넘겨서 기본값(`true`, 자동실행) 그대로 유지된다.
  - **`createPlayground`의 `tabbed` 옵션 — 모든 미션(입문 포함) + 자유 연습 공통 적용(2026-07-18 미션에 적용, 2026-07-24 sandbox.html까지 확장).** 처음엔 주니어/시니어만 바꿀 생각이었는데, 사용자가 파일럿 고급 에디터의 탭 UI를 보고 "이왕 하는 김에 입문도 이 구조로"라고 요청해서 **`tabbed`(html/css/js를 탭으로 전환하며 보여줌)와 `autoRun`(입력하는 대로 바로 반영할지)을 서로 독립된 옵션으로 분리**했다 — `mission-page.js`는 모든 미션에 `tabbed: true`를 준다. `sandbox.html`(자유 연습)도 처음엔 예전 3칸 스택 방식으로 남겨뒀었지만("자유 연습은 성격이 달라 그대로 둠"이라 판단했었음), 이후 사용자가 "미션 형식으로 통일해달라"고 요청해서 `sandbox.html`도 `tabbed: true`로 바꿨다 — 이제 `panels`(자유 연습은 여전히 기본값인 html/css/js 세 칸 다) 외엔 미션 페이지와 동일한 UI다.
  - **`autoRun`은 결국 모든 미션에서 다시 `true`(기본값, 아예 옵션을 안 넘김)로 되돌아옴(2026-07-18, 같은 세션 안에서 두 번 바뀜).** 처음엔 "주니어/시니어는 타이핑 중 미완성 상태가 미리보기에 깜빡이는 게 거슬린다"는 이유로 주니어/시니어만 버튼 방식으로 바꿨는데, 사용자가 다시 생각해보고 "그 깜빡임의 근본 원인(중괄호를 안 닫은 채로 잠깐 있는 것)을 고치면 되지 않냐, 그리고 어차피 홈 화면 문구('입력하면 바로 옆에서 결과를 확인')와도 자동실행이 맞다"고 판단해서, 아래 `maybeAutoCloseBracket`로 근본 원인을 고치고 **`autoRun`은 전부 되돌려서 다시 전체 자동실행으로 통일**했다. `autoRun` 옵션 자체(코드)는 `editor.js`에 그대로 남아있다 — 지금은 아무도 `false`를 안 넘길 뿐, 나중에 다시 필요해지면 옵션만 넘기면 된다.
  - **CSS/JS 칸에서 "{"를 치면 "}"가 자동으로 생성된다 — `maybeAutoCloseBracket`, `editor.js`(2026-07-18, 사용자가 명시적으로 요청).** `maybeAutoCloseTag`(HTML 태그 자동 닫기)와 똑같은 패턴·같은 이유: 닫는 중괄호를 깜빡하는 걸 애초에 막는다. `AUTO_CLOSE_BRACKETS = { "{": "}" }`로 범위를 **`{`/`}` 하나로만 일부러 좁혔다** — `(`/`[`도 넣을 수 있었지만, 특히 `(`는 함수 호출(`querySelector(...)` 등)에서 훨씬 자주 쓰이는데 "닫는 괄호를 직접 타이핑해서 넘어가는"(type-over) 처리까지 없이 자동 삽입만 하면 오히려 중복 괄호(`)…))`)가 더 자주 생길 위험이 있어서 이번엔 안 넣었다(필요해지면 type-over 처리까지 같이 만들 것). 이 기능은 탭 모드(`createPlayground`)와 고급 에디터(`advanced-editor.js`) 양쪽 다 적용되어 있다.
    - **주의: "닫는 중괄호/세미콜론이 없어도 CSS가 적용되는 게 이상하다"는 지적은 절반만 맞다 — 채점/미리보기 로직을 억지로 엄격하게 만들지 않기로 했다.** CSS 스펙 자체가 "파일 끝(EOF)에서 안 닫힌 블록은 거기서 닫힌 것처럼 처리", "블록의 마지막 선언은 세미콜론 생략 가능"을 정식으로 허용한다 — 그래서 규칙이 하나뿐이고 파일 맨 끝에서 `}`를 빠뜨린 경우는 실제 브라우저에서도 원래 정상 동작하는 게 "버그"가 아니라 사실이다. 진짜로 깨지는 경우는 **중간에 있는 `}`를 빠뜨려서 다음 규칙의 선택자까지 이전 블록 안으로 삼켜지는 경우**인데, 이건 `maybeAutoCloseBracket`가 애초에 `{`를 칠 때마다 `}`를 자동으로 만들어주기 때문에 실질적으로 거의 안 생긴다 — 그래서 "근본 원인(편집기)"을 고치는 쪽을 택했고, `parseCssRules`/채점 로직 쪽에 별도의 엄격한 문법 검증을 추가하지는 않았다(그렇게 하면 오히려 스펙상 정당한 CSS를 "틀렸다"고 잘못 가르치게 됨).
  - 구현은 DOM 구조가 완전히 다른 두 모드(스택 모드는 칸마다 `<textarea>`가 따로, 탭 모드는 공유 `<textarea>` 하나를 탭 전환마다 내용만 바꿔치기 — `assets/js/advanced-editor.js`의 탭 로직과 동일한 패턴)를 함수 안에서 분기해서 `currentValue`/`updateVisual`/`syncScroll`/`bindEditorEvents`에 각각 대입해뒀다 — `run()`/`notifyChange()`/`getCode()`/`setCode()`는 이 함수들을 통해서만 값을 주고받아서 모드를 몰라도 되게 만들었다. 탭 모드는 탭을 바꾸기 직전에 반드시 `flushActive()`로 지금 텍스트를 메모리에 반영해야 한다(안 하면 탭 전환 시 타이핑한 내용이 사라진 것처럼 보이는 버그).
  - **전체화면은 에디터 쪽만이 아니라 `.playground` 전체(에디터+미리보기 두 칸 다)를 확장한다** — 사용자가 "양쪽 레이아웃은 그대로 유지하면서" 크게 보고 싶다고 명시적으로 요청함. `.playground.fullscreen { position:fixed; inset:0; }`로 그리드 두 칸 구조를 유지한 채 뷰포트 전체를 채운다(파일럿 고급 에디터의 전체화면이 에디터 영역만 확장했던 것과 다른 점 — 용도가 다름: 그쪽은 파일 여러 개를 크게 보는 용도, 이쪽은 에디터+결과를 같이 크게 보는 용도).
- **고급(멀티파일) 에디터 — `assets/js/advanced-editor.js`, `createAdvancedPlayground(root, options)`** (2026-07-18 신설). 기존 `createPlayground`는 미션 하나당 HTML/CSS/JS 딱 세 칸뿐이라 **여러 파일로 나눠서 import/export로 연결하는 걸 절대 가르칠 수 없다**는 공백 때문에 만듦 — 지금은 파일럿 미션 `js-module-import-intro` 하나만 이 에디터를 쓴다(미션 객체에 `advancedEditor: true`, `fileOrder`, `entry`, `bodyHtml` 필드로 표시; `starter`/저장 코드도 `{html,css,js}`가 아니라 `{files: {파일명: 내용}}` 모양). `mission-page.js`가 이 필드를 보고 `createPlayground` 대신 이 함수를 호출하도록 분기되어 있다. **기존 주니어/시니어 미션 전체를 이 에디터로 옮기는 건 아직 안 함** — 이 파일럿이 잘 되는지 보고 사용자와 별도로 상의하기로 함.
  - 화면 구성: 파일마다 탭 하나(클릭으로 전환, textarea/강조/줄번호 요소는 하나뿐이고 활성 탭이 바뀔 때마다 그 안의 내용만 바꿔치기함) + 전체화면 토글 버튼(CSS 클래스 `.fullscreen`로 `position:fixed;inset:0` 오버레이 — Fullscreen API 대신 이 방식을 씀, 권한 프롬프트 없이 더 안정적이라 이 사이트가 지금까지 브라우저 API보다 직접 DOM/CSS 제어를 선호해온 것과 일관됨) + 오른쪽 아래 고정 "실행 ▶" 버튼(누르기 전엔 결과 안 보임, 누르면 프리뷰 오버레이가 열림).
  - 문법 강조(`highlightHtml`/`highlightCss`/`highlightJs`)와 에디터 동작(`maybeAutoCloseTag`, `handleEnterKey`, `maybeOutdentClosingBracket`, `handleTabKey`)은 `editor.js`의 전역 함수를 그대로 재사용한다 — 전부 특정 파일 하나에 종속되지 않고 `textarea` 하나를 인자로 받는 범용 함수라 가능했다. `mission.html`에서 `advanced-editor.js`는 `editor.js` 다음, classic `<script>`로 로드된다(이 함수들에 의존하므로 순서 중요).
  - **`.advanced-preview-overlay`를 안 보이게 할 때 `hidden` 속성이 아니라 `.show` 클래스로 토글한다.** `[hidden]`의 기본 스타일(`display:none`)은 UA(브라우저 기본) 스타일이라, author CSS가 그 요소에 `display:flex`를 지정해두면(오버레이를 flex로 배치해야 해서 필요함) specificity가 같아도 **author 스타일이 origin 우선순위 때문에 무조건 이긴다** — 그러면 `hidden` 속성을 줘도 계속 보이는 버그가 생긴다. 그래서 기본은 `display:none`으로 두고 `.show` 클래스가 붙었을 때만 `display:flex`로 바꾸는 방식을 쓴다. 앞으로 `hidden` 속성으로 뭔가를 숨기는 UI를 새로 만들 때 이 함정을 기억할 것.
  - **핵심 함정 — 번들러 없이 브라우저에서 진짜 cross-file `import`를 실행시키는 방법**: 각 파일을 `Blob`으로 만들어 `URL.createObjectURL()`로 blob: URL을 발급하고, 소스 안의 `import ... from "./파일명"` 경로를 그 blob: URL로 치환한 뒤 진입점을 동적 `import()`한다. **이 Blob 생성은 반드시 iframe "내부"에서 해야 한다.** 부모 페이지(우리 스크립트)에서 Blob을 만들면 그 blob: URL은 부모 origin에 묶이는데, 이 사이트의 미리보기 iframe은 (보안 원칙상) `allow-same-origin` 없이 `srcdoc`으로 로드되어 매번 새로운 opaque origin을 갖기 때문에, iframe 입장에선 그 blob: URL이 "남의 것"이라 못 불러온다 — `import`라서 생기는 문제가 아니라, 지금까지 이 사이트가 JS를 항상 인라인 `<script>{js}</script>`로만 넣어봤지 `src=`로 외부 리소스를 fetch해본 적이 없어서 여태 안 드러났던 함정이다. 그래서 `srcdoc`에 먼저 실행되는 부트스트랩 `<script>`를 심어서, **그 스크립트가 iframe 내부 컨텍스트에서** 각 파일의 Blob을 만들고(MIME 타입은 `"text/javascript"`로 명시 — 모듈 스크립트는 classic script보다 MIME 검사가 엄격함) import 경로를 즉석 blob: URL로 치환한 뒤 진입점을 `import()`하는 구조로 만들었다(`buildAdvancedSrcdoc`/`BOOTSTRAP_RUNNER_SRC`). 스파이크로 이 구조가 `sandbox="allow-scripts"`(allow-same-origin 없음) 안에서 실제 동작하는 것까지 확인했다 — **보안 원칙(iframe에 `allow-same-origin` 절대 안 줌)은 그대로 유지한 채로 문제를 풀었다.**
  - import 경로 치환 정규식은 일부러 좁게 잡는다 — `options.files`에 실제로 존재하는 파일명과 정확히 일치하는 `"./파일명"`/`'./파일명'` 형태만 치환하고, 그 외 형태(별칭 import, 상대 경로 `../`, 패키지 import 등)는 지원하지 않는다. 진짜 ESM 파서가 필요 없는, 초보자가 쓸 범위 안에서만 동작하면 충분하다는 판단.
  - 탭을 전환하거나 `run()`/`getCode()`를 부르기 직전엔 반드시 지금 활성 textarea의 값을 메모리상 `files` 맵에 반영(flush)한다(`flushActiveFile`) — 안 하면 탭을 전환할 때 방금 타이핑한 내용이 사라진 것처럼 보이는 버그가 생긴다.
  - `storage.js`의 `saveCode`/`getSavedCode`는 **전혀 수정하지 않았다** — 원래도 `code`를 그대로 JSON 저장/복원하는 범용 구조라 `{files:{...}}` 모양도 그대로 저장된다. `advancedEditor` 미션에는 `startFromMission`(이전 미션 HTML 이어받기) 개념이 해당 없다.
  - 새 `advancedEditor` 미션을 추가하려면: 미션 파일에 `advancedEditor: true`, `fileOrder`(탭 순서 배열), `entry`(실행 진입점 파일명), `bodyHtml`(실행 시 쓰이는, 학습자가 못 건드리는 고정 HTML), `starter.files`(파일명→시작 코드, 정답 골격 없이)를 넣고, `check(code)`에서 `code.files["파일명"]`을 문자열/정규식으로 검사한다(다른 미션들과 똑같이 실행 결과 훔쳐보기 없음).
- **미션 페이지의 버튼 배치**: `목록으로`는 왼쪽, `채점하기`(+ 피드백 메시지)는 오른쪽 — `.mission-actions`를 `justify-content: space-between`으로 양 끝에 배치하고, 오른쪽 그룹(`feedback` + `check-btn`)만 `.mission-actions-right`로 따로 묶는다. **"채점하기" 버튼은 통과하면 새 버튼을 옆에 만드는 게 아니라, 같은 버튼이 그 자리에서 "다음 문제로 →"로 바뀐다** (`mission-page.js`의 `switchToNextMode`, `checkBtn.dataset.mode`로 상태 구분). 다음 미션은 `MISSIONS` 배열의 다음 인덱스(tier 필터 무관, 커리큘럼 순서). 마지막 미션이면 다음이 없으니 "🎉 모든 미션 완료!"로 바뀌며 버튼이 비활성화된다. **이미 통과한 미션을 다시 열면(`getProgress()`에 기록 있음) 채점 버튼을 다시 누를 필요 없이 처음부터 "다음 문제로" 상태로 렌더링된다** — 안 그러면 다시 열 때마다 매번 채점하기부터 눌러야 해서 불편하다.
- `missions.html`의 `PAGE_SIZE`는 4의 배수로 유지한다 — 카드 그리드가 보통 한 줄에 여러 칸(주로 4칸 안팎)으로 채워지는데, 4의 배수가 아니면 마지막 페이지의 마지막 줄이 어중간하게 비어 보인다(예: 6은 두 번째 줄에 2칸만 차고 2칸이 빔). 미션이 늘어나서 `PAGE_SIZE`를 다시 조정할 일이 있어도 이 원칙을 유지할 것.

## 커리큘럼 단계별 목표 (사용자가 정의, 2026-07-18)

각 단계를 "이론상 완벽히" 끝냈을 때 방문자가 도달해야 하는 상태. 새 미션을 설계할 때 이 목표에 비춰 뭐가 빠졌는지 판단할 것.

**미션 개수 원칙(사용자가 명시적으로 지적, 2026-07-18): 최종적으로는 항상 입문 &lt; 주니어 &lt; 시니어 순으로 미션 개수가 많아져야 한다.** 지금처럼 한창 만들어가는 중에는 이 순서를 안 지켜도 되지만(지금은 입문이 제일 많음), 커리큘럼이 완성되어갈수록 결과적으로 시니어 단계의 미션이 가장 많고, 그다음 주니어, 그다음 입문 순으로 적어지는 모양이 되어야 한다. 새 미션 배치를 정할 때 이 방향을 염두에 둘 것(예: 애매하면 시니어/주니어 쪽을 조금 더 두텁게).

- **입문**: 코드를 하나도 모르는 사람 → 코드를 읽고 수정할 수 있는 사람이 되는 게 목적. 실제로 자주 쓰이는 HTML 태그를 최소 한 번씩은 다뤄보고 각 태그가 어떤 효과를 내는지 안다. HTML 안에 CSS/JS를 넣는 방법(내부 `<style>`/`<script>`, 외부 파일 연결)과 프로젝트 폴더 구조(`assets/css`, `assets/js` 등)를 이해한다. **레이아웃(flexbox/grid)도 여기 포함 — `css-flexbox-row-intro`/`css-flexbox-justify-align-intro`/`css-grid-intro`(2026-07-18 추가)로 이 사이트 자체가 실제로 쓰는 `display:flex`(헤더 좌우 배치)와 `display:grid`(카드 목록) 패턴을 박스모델 바로 다음, JS 시작 전에 가르침.** 박스모델(요소 하나의 안팎 여백)과 레이아웃(여러 요소를 어떻게 배치할지)을 잇는 자연스러운 다음 단계라 이 위치에 둠.
- **주니어**: 입문을 넘어 이 사이트(CODE-LEARN) 자체를 스스로 만들 수 있는 수준. 다크모드 토글, 필터, 페이지네이션, 카드 목록 등 이 사이트에 실제로 쓰인 요소를 전부 다루고 응용할 수 있다. (주니어 미션 만들 땐 그 컴포넌트에 필요한 개념을 먼저 입문 단계의 잘게 쪼갠 미션으로 깔아둘 것.) **다크모드(`junior-dark-mode-toggle`), 필터(`junior-filter-buttons`), 카드 목록(`junior-card-list`), 페이지네이션(`junior-pagination`) 전부 완료(2026-07-18).** 각각의 선행 개념: 필터는 `js-array-loop-intro`(배열+forEach)에서 배열/forEach를, 카드 목록은 `js-object-basics`(객체 `{키:값}`)에서 객체를, 페이지네이션은 `js-array-slice-intro`(`slice`)에서 배열 자르기를 먼저 깔아둠 — 페이지네이션은 그 위에 `Math.ceil`과 "상태를 변수에 저장하고 바뀔 때마다 다시 그리는(render 함수)" 패턴을 새로 가르침(카운터 버튼처럼 DOM에서 값을 매번 읽어오는 방식과 다름). 이 사이트가 실제로 쓰는 주니어급 UI 패턴은 사실상 다 커버됨 — 다음 주니어 확장은 새 컴포넌트 발굴이 필요.
- **시니어**: 전체 목표는 아직 미정이지만, **첫 주제는 웹 보안으로 확정함(2026-07-18)** — "클라이언트 쪽에서 뭔가를 막는 건 진짜 보안이 아니다"를 핵심 축으로 삼는다(복붙 방지, CSS content로 숨기기 같은 걸 다 뚫을 수 있다는 사용자 지적에서 출발함). `innerHTML`(XSS), `eval()` 같은 "이렇게 하면 편해 보이지만 위험한 패턴 → 안전한 대안"을 다루는 미션들로 시작했음(`senior-xss-textcontent`, `senior-eval-danger`). 다음 보안 주제 후보: 클라이언트 검증은 UX일 뿐 보안이 아니다(서버 검증 필요), URL/href에 `javascript:` 스킴이 들어갈 때의 위험 등 — 이 사이트에 서버가 없어서 "실제로 타이핑해서 체험"하는 형태로 만들기 어려운 주제는 신중히 설계할 것.

**설계상 주의할 점(아직 구조적으로 못 채운 구멍)**: 지금 플레이그라운드는 HTML/CSS/JS 3칸이 분리되어 있고 `editor.js`가 자동으로 `<style>`/`<script>`로 감싸서 미리보기에 넣어준다. 그래서 방문자가 실제로 `<style>`/`<script>` 태그나 `<link rel="stylesheet">`를 직접 타이핑해볼 기회가 아직 없다 — "HTML에 CSS/JS를 넣는 법" 목표를 채우려면 HTML 칸에 직접 이런 태그를 쓰게 하고 `DOMParser`로 채점하는 별도 미션이 필요하다. 폴더 구조/외부 파일 연결은 이 사이트가 단일 파일 샌드박스라 실제 여러 파일을 만들어볼 수 없으므로, 코드 채점보다는 설명 위주(그림/글)로 가르칠 가능성이 높다.

## 새 미션 추가하는 법

1. `assets/js/missions/` 안에 새 파일 하나를 만든다(파일 이름 = 미션 id, 예: `css-flexbox-intro.js`). 다른 미션 파일 하나를 그대로 참고해서 구조를 맞추면 된다: `id`, `title`, `level`, `panels`(이 미션까지 배운 언어만), `summary`, `instructions`(HTML 문자열 — 구체적인 예시 코드를 `<pre><code>`로 포함할 것), `starter`(`panels`에 있는 키만 넣기 — 위 규칙대로 정답 골격 없이 빈 값/안내 주석), `check(code)`(`{pass, message}` 반환)를 담은 객체를 `export default`한다. CSS 파싱 헬퍼(`findRuleForTag` 등)가 필요하면 파일 맨 위에서 `import { ... } from "./helpers.js";`.
2. `assets/js/missions-data.js`에 그 파일을 import하는 줄과, `MISSIONS` 배열 안에 커리큘럼상 알맞은 위치(항상 맨 끝은 아님)에 한 줄을 추가한다.
3. **새 미션의 `starter`/`instructions`/데모(iframe)가 쓰는 HTML 태그가 전부 그 위치 이전의 미션에서 이미 가르쳐졌는지 반드시 확인할 것.** 실수하기 쉬운 지점이다 — 실제로 두 번 겪었다: `css-highlight-selector`(3번째 미션)가 아직 안 가르친 `ul`/`li`를 썼던 것(→ `p`로 교체해서 고침), `css-border-intro`가 아직 안 가르친 `div`를 썼던 것(→ `html-div-span-basics`를 그 앞으로 옮겨서 고침). 새 미션을 배열 중간에 끼워 넣을 때마다 그 미션이 사용하는 태그 목록과, 그 위치까지 배열 순서상 이미 등장한 "정식으로 가르친" 태그 목록을 대조해볼 것.
4. 그게 끝. `missions.html`과 `mission.html`은 `MISSIONS` 배열을 순회/조회하므로 자동으로 반영된다.

## 저장 방식 (localStorage → 서버 전환 가이드)

지금은 전부 `assets/js/storage.js` 안에 localStorage로 구현되어 있다. 이 파일이 유일한 "저장소 어댑터"이고, 이걸 호출하는 쪽(`missions.html`, `mission-page.js`)은 저장 방식이 localStorage인지 서버인지 전혀 몰라도 되게 일부러 분리해뒀다.

- `getProgress()` → `{ missionId: true, ... }` (완료한 미션 목록)
- `saveProgress(missionId)` → 해당 미션을 완료 처리
- `getSavedCode(missionId)` → `{ html, css, js } | null` (그 미션에서 마지막으로 저장된 코드. **사용자가 실제로 타이핑하거나 "실행"/"채점하기"를 눌렀을 때만** debounce로 자동 저장됨 — `editor.js`의 최초 렌더링(`run()`)에서는 절대 저장을 호출하면 안 된다. 페이지를 열기만 해도 저장되게 만들면, 그 즉시 "이미 이 미션을 작업한 것"으로 취급돼서 `startFromMission` 이어받기가 다시는 동작 안 하는 버그가 생긴다 — 실제로 한 번 겪었던 버그.)
- `saveCode(missionId, code)` → 코드 저장

나중에 진짜로 여러 사람이 같은 계정으로 여러 기기에서 이어서 하게 만들고 싶어져서 서버로 옮기게 되면:

1. **사용자 식별 수단이 먼저 필요하다.** 지금은 "이 브라우저"가 곧 사용자 단위라 로그인 개념이 아예 없다. 서버로 가면 로그인/세션 같은 걸 새로 만들어야 하는데, 이건 이 정적 사이트 구조를 벗어나는 큰 결정이라 별도로 사용자와 상의하고 진행할 것.
2. **`storage.js`의 네 함수 내부만 `fetch` 호출로 바꾼다.** 예: `GET /api/progress`, `PUT /api/progress/:missionId`, `GET /api/code/:missionId`, `PUT /api/code/:missionId`. 함수 시그니처(입력/출력 모양)를 그대로 유지하면 `mission-page.js`나 `missions.html` 쪽은 한 줄도 안 고쳐도 된다 — 이게 이 구조를 이렇게 나눠둔 이유다.
3. 저장되는 코드는 방문자가 직접 입력한 HTML/CSS/JS 텍스트다. 서버에 저장하게 되면: 저장 전에 적당한 길이 제한을 두고, 만약 나중에 "다른 사람 코드 구경하기" 같은 기능을 만들 경우 저장된 코드를 그대로 실행/렌더링해서 보여줄 때는 지금 sandbox iframe과 똑같이 `sandbox="allow-scripts"`만 쓰고 `allow-same-origin`은 주지 않는 원칙을 반드시 유지할 것 (안 그러면 한 방문자가 만든 악성 코드가 다른 방문자를 볼 때 실행되는 저장형 XSS가 된다).

## MEMORY.md / CLAUDE.md 비대해지지 않게 관리하기 (UPDATE 절차)

이 두 파일은 매번 세션 컨텍스트에 들어가므로 짧게 유지해야 한다. 아래 상황이 되면 정리(컴팩션)한다:

- **MEMORY.md가 대략 40줄을 넘거나, 이미 끝난 작업 로그가 쌓였을 때**: 완료되어 더 이상 의사결정에 영향 없는 항목은 지우고, "현재 상태" + "다음에 할 일" 정도의 짧은 요약만 남긴다. 지워도 되는지 애매한 굵직한 결정(예: 채점 방식, 보안 정책)은 MEMORY.md가 아니라 위 "아키텍처 핵심 결정" 섹션(CLAUDE.md)으로 승격시켜 영구 보존한다.
- **CLAUDE.md가 커지면**: 이 문서는 "지금 구조가 어떻게 되어 있고 왜 그런지"만 담고, 진행 상황/TODO는 전부 MEMORY.md로 보낸다. 둘의 역할이 섞이지 않게 유지 — CLAUDE.md = 불변에 가까운 아키텍처/정책, MEMORY.md = 휘발성 있는 현재 상태.
- 정리할 때는 먼저 이 파일들을 읽고, 사용자에게 뭘 지웠는지 간단히 알려준 뒤 진행한다.
